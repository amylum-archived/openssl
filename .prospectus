item do
  expected do
    url_xpath
    url 'https://www.openssl.org/source/'
    xpath '//*[@id="content"]/div/article/div/table/tr[5]/td[3]/a[1]'
    regex /^openssl-([\w.]+).tar.gz$/
  end

  actual do
    git_tag
    regex /^(.*)-\d+$/
  end
end
